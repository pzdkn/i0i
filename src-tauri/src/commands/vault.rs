//! Vault-scoped commands.
//!
//! RFC 0087 R3: `get_vault_status` used to live here returning the literals
//! 234 papers / 12 unread. The status bar derives its counts from the library
//! the frontend already holds, so a count can no longer disagree with the list
//! it counts. Nothing else needed this module yet.
